# Infrastructure

Infrastructure configuration will live here.

The PRD + SES requires separate development, testing, staging, and production environments with PostgreSQL, Redis, background workers, object storage, monitoring, logging, health checks, and migration support.
