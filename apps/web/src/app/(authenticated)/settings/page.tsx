import { SettingsWorkspace } from "../../../components/settings/settings-workspace";

export const metadata = {
  title: "Settings | Salense",
};

export default function SettingsPage() {
  return <SettingsWorkspace />;
}
