import { SubscriptionSettingsWorkspace } from "../../../../components/subscription/subscription-settings-workspace";

export const metadata = {
  title: "Subscription | Salense",
};

export default function SubscriptionPage() {
  return <SubscriptionSettingsWorkspace />;
}
