import { CustomersWorkspace } from "../../../components/customers/customers-workspace";

export const metadata = {
  title: "Customers | Salense",
};

export default function CustomersPage() {
  return <CustomersWorkspace />;
}
