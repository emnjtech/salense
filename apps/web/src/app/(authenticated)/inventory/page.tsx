import { InventoryWorkspace } from "../../../components/inventory/inventory-workspace";

export const metadata = {
  title: "Inventory | Salense",
};

export default function InventoryPage() {
  return <InventoryWorkspace />;
}
